<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evilgeniuses.lifecare">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TestActivity"></activity>
        <activity android:name=".activities.TestListActivity" />
        <activity android:name=".activities.WebViewActivity" />
        <activity android:name=".activities.GraphicActivity" />
        <activity android:name=".activities.pulse.PulseActivity" />
        <activity android:name=".activities.MonitoringActivity" />
        <activity android:name=".activities.DiaryActivity" />
        <activity android:name=".activities.CheckListActivity" />
        <activity android:name=".activities.NavigationDoctorActivity" />
        <activity android:name=".activities.NavigationNurseActivity" />
        <activity
            android:name=".activities.AuthenticationActivity"
            android:theme="@style/AuthenticationTheme" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>